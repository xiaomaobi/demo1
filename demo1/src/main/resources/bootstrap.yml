server:
  port: 8888
spring:
  application:
    name: demo1
  cloud:
    nacos:
      server-addr: 8.140.24.77:8848  #nacos服务地址
      #注册中心
      discovery:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
      #      discovery:
      #        namespace: 6c02d65d-402a-486e-b683-5c88cf37b25a
      #配置中心
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        #        namespace: 6c02d65d-402a-486e-b683-5c88cf37b25a   #我们在nacos中创建的空间名称
        timeout: 3000
        refresh-enabled: true  #修改后自动加载
        group: dev   #nacos配置文件所选择的group
        #        prefix: ${spring.application.name}
        file-extension: yaml  #读取nacos配置文件后缀
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:p6spy:mysql://39.99.184.223:3306/test?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=Asia/Shanghai&tinyInt1isBit=false&allowMultiQueries=true
    username: sdzquery1
    password: sdz@2021
      #JDB连接池配置
    hikari:
      pool-name: spring-demo-1 #连接池名称
      connection-timeout: 60000 #请求连接的超时时间
      maximum-pool-size: 20 #最大连接数量
      max-lifetime: 1800000 #连接的最长生命周期
      keepalive-time: 1750000 #尝试保持连接的频率
      connection-test-query: select 1 #连接验证sql

